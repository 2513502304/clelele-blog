---
import { bangumiConfig } from '@constants/site-config';
import { getLocaleStaticPaths } from '../_shared/utils';
import BangumiPage from '../bangumi.astro';

export const getStaticPaths = bangumiConfig ? getLocaleStaticPaths : () => [];
---
<BangumiPage />
