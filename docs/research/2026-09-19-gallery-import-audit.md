# Gallery 导入计数与历史尺寸统计

本文保留 2026-09-19 核查的可复现计数，2026-09-23 精简。HF Catalog 快照更新时间为 `2026-09-19T04:50:41.523Z`；后续补图和新增收藏已经改变线上数据，下表不是当前库存。

## 40 条记录为什么生成 38 张卡片

9 月 18 日会话 `01a0b346-d990-7760-9a0f-3f98d1eef33f` 提取出 40 条有效图片/Prompt 记录，每条一张图片，共 38 个不同图片字节哈希。

| 图片 hash | 图片所在 JSONL 行 | Prompt 所在行 | 结果 |
| --- | --- | --- | --- |
| `1e06129f4bdf` | 376、601 | 395、622 | 图片与规范化 Prompt 均相同，跳过后一条 |
| `3bd039472939` | 276、910 | 295、929 | 图片相同、Prompt 不同，一张卡片保留两条变体 |

因此 `40 - 1 条完全重复 = 39 条 Prompt`，其中两条属于同一张图片，所以是 38 张卡片。`Uploaded 76` 表示对象文件数：38 张原图 + 38 张 WebP 缩略图，不是 76 张原图，也没有漏传两张独立图片。新日志列出重复/变体的卡片 slug、图片行、Prompt 行、匹配来源，并将 Identity / Plan / Prepare / Metadata / Result / Tags 用空行分开。

## 大文件读取错误

9 月 17 日会话 `01a0af76-a8ca-7281-9c00-6145b1362399` 大小为 1,257,248,449 字节。原先 `readFile(..., 'utf8')` 试图分配整个文件的单一字符串，实测抛出 `RangeError: Invalid string length`。

改为逐行流式解析，读取时立即运行原有 turn/附件/Prompt 配对状态机，不保留工具输出等无关记录。真实文件成功提取 468 条有效记录，首末图片行号为 9 / 9294，本机提取约 1.42 秒、结束时 RSS 约 917 MiB。该内存数不是峰值保证：有效图片仍需保留用于后续去重/上传，流式读取并不等于固定内存。空行计入物理行号；坏 JSON 报告准确行号。

当时的只读预检查得到 344 张新卡候选、2 张已有卡包含新 Prompt、122 条重复记录，计划准备 688 个资产文件。这是当时的计划，不能用作后续实际导入数量。

npm 导入入口仅通过 `--disable-warning=DEP0205` 屏蔽当前 tsx 加载器触发的该条弃用警告，其他警告和错误仍然显示，不更换运行时加载机制。

## HF 源图尺寸快照

当时 3711 张卡片包含 3712 张独立源图，其中一张卡片有两图。尺寸来自元数据，缺失数为 0；没有为这项统计下载或解码全部源文件。缩略图与 sub-images 不计入。

判定为 `max(width,height)>2048`，或 `ceil(width/32)×ceil(height/32)>2500`。这是与 Codex High 模型预算作比较，不是本站存储限制；超限不能证明模型没处理，也不是缩小 HF 原图的理由。

| 导入月份（UTC+8） | 原图数 | 最大边超限 | 图块数超限 | 至少一项超限 |
| --- | ---: | ---: | ---: | ---: |
| 2026-06 | 21 | 4 | 9 | 9 |
| 2026-07 | 954 | 325 | 481 | 483 |
| 2026-08 | 1667 | 499 | 818 | 826 |
| 2026-09 | 1070 | 145 | 159 | 159 |
| **合计** | **3712** | **973** | **1467** | **1477** |

逐日数据见 [CSV](2026-09-19-gallery-image-dimensions-by-date.csv)。日期按当时统计使用的卡片导入字段分组，不能拿它确定用户切换客户端或开始缩放的时间。恢复附件、手动收藏和不同客户端都可能出现在同一天。

## 怎样判断客户端是否保留原图

尺寸符合预算不证明发生过压缩，超过预算也不证明模型直接收到原图。必须区分 JSONL 的 UI 图片和模型投影，再与磁盘文件比较。会话跨客户端续接时，`source`、`originator`、记录版本均不足以单独标识每条输入；升级终端 CLI 也不等于更新扩展内置二进制。

[十张原文件实验](2026-09-20-codex-ten-originals-verification.md)已经直接验证：本次 VS Code 五张 UI 图片全部保留原始字节；App 五张中四张改变，一张不变，立即发送也可能缩小。该结果替代根据图标、日期分布或客户端名称推断原图质量的做法。编码与去重规则见[统一说明](2026-09-19-codex-image-preprocessing.md)。
